import React from 'react';

const Addtocart = ({Cart}) => {
    const {name} =Cart;

    let total = 0;
    for(cos)
    return (
        <div>
            <h1>This is cart{name}</h1>
        </div>
    );
};

export default Addtocart;