import React from 'react';

const Addtocart = (props) => {
  const {Cart}=props
  
    return (
        <div>
            <h1>This is cart:{total}</h1>
        </div>
    );
};

export default Addtocart;