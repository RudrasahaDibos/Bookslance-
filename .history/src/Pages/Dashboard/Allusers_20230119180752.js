import React from 'react';
import { useQuery } from 'react-query';

const Allusers = () => {
const { } = useQuery

    return (
        <div>
            
        </div>
    );
};

export default Allusers;